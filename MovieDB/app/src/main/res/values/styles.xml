<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">?android:attr/colorBackground</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@android:color/secondary_text_light</item>
    </style>

    <style name="m" parent="Theme.AppCompat.Light.NoActionBar" >
        <item name="android:colorForeground">@color/cardview_shadow_start_color</item>
    </style>

</resources>
